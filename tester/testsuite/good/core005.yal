/* usage of variable initialized in both branches. */

fn main () -> int {
    let x: int = 0;
    let y: int = 56;
  if (y + 45 <= 2) {
    x = 1;
  } else {
    x = 2;
  }
  printInt(x);
  return 0 ;

}
