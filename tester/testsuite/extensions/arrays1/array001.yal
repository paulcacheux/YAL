fn main() -> int {

let a = [0; 10];
let j: int = 0;
  while (j<a.length) {
     a[j] = j;
     j++;
  }

    for (let i = 0; i < a.length; i++)
        printInt(a[i]);

let x: int = 45;
  printInt(x);
  return 0;
}
